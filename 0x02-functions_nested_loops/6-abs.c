#include <stdio.h>
#include "main.h"
#include <stdlib.h>
/**
 * _abs - function to compute the sbolute value of int
 * @n: checking value 
 * Return: 0
 *
 */
int _abs(int n)
{
	n = abs(n);
	return (n);
}
